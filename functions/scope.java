// method scope -> can use variable after defination not before
// variable only can use in that declared func, to use in other only can use as parameter

// block scope -> variable in block can only use in that block
public class scope{
    public static void main(String[] args) {
        
    }
}

